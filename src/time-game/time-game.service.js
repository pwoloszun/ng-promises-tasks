App.factory("timeGame", [
  "$q",
  function ($q) {

    // TODO
    var timeGame = {
      game: function () {
      },
      finish: function (result) {
      },
      quit: function () {
      }
    };

    return timeGame;
  }
]);
