App.controller("BatteryChargingPageController", [
  "$scope",
  "battery",
  function ($scope, battery) {
    //TODO: battery
    // battery.getManager().then(function (data) {
    //   var className = "fa fa-battery-" + Math.round(data.level * 4);
    // });
  }
]);
