App.factory("gmaps", [
  "$http",
  function ($http) {
    var URL = "https://maps.googleapis.com/maps/api/geocode/json";

    // TODO
    // coord params: {latlng: coords.latitude + ',' + coords.longitude}
    // addr params: {address: address}

    return null;
  }
]);
