App.factory("fullWeather", [
  "geoLocator",
  "gmaps",
  "openWeather",
  function (geoLocator, gmaps, accuWeather) {
    //TODO
    var fullWeather = {
      weather: function () {
        // return promise;
      }
    };
    return fullWeather;
  }
]);
